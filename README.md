# -Shop-Product-CRUD-Application
here is a simple, runnable Python command-line application that performs the same CRUD (Create, Read, Update, Delete) operations by storing the product data in memory (a list of dictionaries).
